import 'package:flutter/material.dart';

class MiddlewaresPage extends StatelessWidget {

  const MiddlewaresPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Middlewares'),),
           body: Container(),
       );
  }
}